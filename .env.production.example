# Never commit the real value. Use a least-privilege MongoDB Atlas user.
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER/conta?retryWrites=true&w=majority
MONGODB_DB=conta
# Generate locally; never commit the generated values.
OWNER_PASSWORD_HASH=
SESSION_SECRET=
PORT=3000
HOSTNAME=127.0.0.1
